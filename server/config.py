# Configuration constants like file paths, model names, and supported packages
# Settings for vector database path, embedding model choice, and API parameters
# Environment-specific configurations (though for university project, just hardcoded values)
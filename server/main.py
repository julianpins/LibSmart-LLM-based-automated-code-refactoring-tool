# FastAPI app creation, CORS setup, and endpoint definitions (/health and /analyze)
# Imports RAGService and Pydantic models, handles HTTP requests/responses
# Routes requests to the RAG service and returns JSON responses

from fastapi import FastAPI